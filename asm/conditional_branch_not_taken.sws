; load special address into %ix
  MOVL %ix, $69
  MOVH %ix, $69

  MOVL %ax, $45
  MOVL %bx, #50
  cmp %ax, %bx
  J.EQ .EQUAL
; If it wasn't 0, print %ax
  STORE %ax, [%ix]
  JMP .DONE

.EQUAL:
  ADD %bx, #10
  STORE %bx, [%ix]
.DONE:
  HALT
