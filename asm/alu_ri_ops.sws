; load special address into %ix
  MOVL %ix, $69
  MOVH %ix, $69

  
  MOVL %ax, $45
  STORE %ax, [%ix]
  ADD %ax, $c
  STORE %ax, [%ix]
  AND %ax, $0F
  STORE %ax, [%ix]
  OR %bx, $0F
  STORE %bx, [%ix]
  LSL %bx, #4
  STORE %bx, [%ix]
  LSR %bx, #4
  STORE %bx, [%ix]
  HALT
